# Project Context (for BMAD agents)

This repository was produced by **RNC** (Reengineering New Code) from a legacy application. It contains BMAD-method planning documents, not application code.

## Provenance

- Source: legacy system (reverse-engineered)
- Generated by: RNC → UIR (Universal Intermediate Representation) → BMAD docs
- RNC workspaceId: `603f473c-d0aa-4d1a-bdb1-6e365371c787`
- Managed entities: 8 · reference tables: 0

## Instructions for the Dev agent

- Treat `docs/prd.md` as the source of truth for behavior.
- Follow `docs/architecture.md` for structure; do not invent a different stack.
- Implement stories under `docs/stories/` one at a time, in order.
- **Reference tables** (none) get a read-only list API and are used as dropdowns inside the managed entities — do NOT scaffold create/edit/delete screens for them.
- Requirements tagged **NEEDS REVIEW** are ambiguous in the legacy source: implement a best-effort version and leave a `// TODO` noting what a human must confirm.
- **Precedence:** author directives (below) > explicit canvas configuration > derived decisions > defaults. When a directive contradicts a derived value, the directive wins.

## Target stack

- Frontend: nextjs
- Backend: nextjs + prisma
- Database: sqlite
- Deployment: docker-compose
- SSO: off

## Derived decisions

Values below were derived automatically from the Architecture Canvas — the agent follows them; the author can override any of them with a directive.

- Backend language: typescript (derived from runtime nextjs)
- Validation: zod (derived from runtime)
- ORM: prisma (from canvas edge label or runtime default)
- Migrations: prisma-migrate (consistent with the ORM)
- Environment: `.env` file with a committed `.env.example` documenting every variable

## RNC MCP guardrail (optional)

Business rules in this pack are cited by their stable key `RK-<12 hex>`. If your coding tool has the RNC MCP server connected (any MCP-capable tool — Claude Code, Cursor, and others — configure it in RNC under Settings → MCP):

- Before marking a story done, call `getRule(workspaceId, "RK-…")` for each rule the story cites and check the implementation against the returned condition and source excerpt.
- For rules tagged **NEEDS REVIEW**, fetch the original legacy source with `getSourceFile` at the rule's location instead of guessing the intent.
- When behavior is unclear, query `searchKnowledge` before inventing anything.

Without MCP the docs are self-sufficient — proceed normally.
